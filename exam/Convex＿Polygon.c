#include <stdio.h>
#include <math.h>

main(){
  int n;
  scanf("%d", %n);
  double a[n], b[n], c[n];
  for(int i=0; i<n; i++){
    scanf("%lf%lf%lf", a[i], b[i], c[i]);
  }

  int x[n], y[n];

  for(int i=0; i<n; i++){
    int next = (i+1)%n
    x[i] = asdf
    y[i] = adsf
  }

}



a[i] + b[i] + c[i]
